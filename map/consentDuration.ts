/**
 * Duration the consent lasts in days
 */
export const consentDurationDays = 30

/**
 * Duration the consent lasts in seconds
 */
export const consentDurationSeconds = consentDurationDays * 24 * 60 * 60

/**
 * Duration the consent lasts in milliseconds
 */
export const consentDurationMS = consentDurationSeconds * 1000
